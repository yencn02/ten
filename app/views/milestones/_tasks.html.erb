<% if tasks.length == 0 then %>
  There is no tasks in this milestone
<% else %>
  <ul id= "tasks">
    <%
    tasks.each do |task|
    %>
      <li><%= link_to task.title, :controller => "tasks", :action => "show", :id => task.id %></li>
    <%
    end
  %>
  </ul>
<% end %>
